% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single_plot.R
\name{plot_NotSBS_iterations}
\alias{plot_NotSBS_iterations}
\title{Visualise Iterative change point Detection Results}
\usage{
plot_NotSBS_iterations(
  results,
  Time,
  m = 3,
  trim,
  threshold = NULL,
  method = c("oracle", "fixed"),
  theta_coef = c(max(0.5 - 10 * log(Time)/Time, 0.42), 0.5, 0.5 + 6 * sqrt(Time)/Time)
)
}
\arguments{
\item{results}{A data frame of intervals with estimated change points and associated statistics.
Must contain columns \code{st} (start), \code{ed} (end), \code{val} (CUSUM value), and \code{est.cp} (estimated change point).}

\item{Time}{An integer specifying the total length of the time series.}

\item{m}{An integer giving the target number of change points for the oracle method. Default is 3.}

\item{trim}{A numeric value used for trimming (not used directly in this function but may be relevant upstream).}

\item{threshold}{A numeric threshold for the fixed method. Required if \code{method = "fixed"}.}

\item{method}{A character string specifying the threshold selection method. Choices are \code{"oracle"} or \code{"fixed"}.}

\item{theta_coef}{A numeric vector of length 3 specifying the relative locations of the true change points, as proportions of \code{Time}.}
}
\value{
A list of ggplot objects corresponding to the selected intervals at each iteration. Plots are also displayed as side-effects.
}
\description{
This function generates a sequence of plots showing the detected change points and their corresponding CUSUM statistics
across iterations of threshold selection, either using a fixed threshold or an oracle-style search.
}
\examples{
\dontrun{
# Assuming `df` is a properly formatted result data frame with st, ed, val, est.cp
plot_NotSBS_iterations(results = df, Time = 300, m = 3, trim = 10, method = "oracle")
}
}
