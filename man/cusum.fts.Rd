% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est_single_cp.R
\name{cusum.fts}
\alias{cusum.fts}
\title{Multiple Change Point Estimation within Seeded Intervals}
\usage{
cusum.fts(x, r = NULL, V.diag = TRUE, lrv = TRUE, n = NULL, trim, lbd = NULL)
}
\arguments{
\item{x}{A matrix of dimension \eqn{p \times T}, representing the observed data with \eqn{p} variables and \eqn{T} dimensionality.}

\item{r}{An optional integer specifying the number of factors. If \code{NULL}, a default data-driven choice is used.}

\item{V.diag}{Logical. If \code{TRUE}, only the diagonal elements of the long-run covariance matrix \eqn{V} are used for standardisation.}

\item{lrv}{Logical. If \code{TRUE}, long-run variance estimation is used with HAC weights up to lag \code{n}.}

\item{n}{Optional bandwidth for HAC estimation. Default is \code{floor(T^{0.25})}.}

\item{trim}{A positive integer specifying the trimming parameter to avoid change point estimation near boundaries.}

\item{lbd}{An optional minimum segment length. If \code{NULL}, a default data-adaptive value is used.}
}
\value{
A data frame with one row per seeded interval and columns:
\describe{
\item{est.cp}{Estimated location of the change point within the interval.}
\item{val}{The associated CUSUM statistic value.}
\item{st}{The interval start index.}
\item{ed}{The interval end index.}
\item{trim}{The trimming parameter used.}
}
}
\description{
This function detects multiple change points in the second moment structure of a high-dimensional time series matrix
using a standardised CUSUM statistic.
}
\examples{
\dontrun{
# Simulate a data matrix with a change in factor covariance
p <- 100; T <- 400
x <- matrix(rnorm(p * T), nrow = p)
cusum.fts(x, trim = 12)
}

}
\seealso{
\code{\link{find_single_cp}} for the CUSUM computation in a single given interval.
}
