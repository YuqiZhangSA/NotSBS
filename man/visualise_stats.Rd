% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/histogram_scaled.R
\name{visualise_stats}
\alias{visualise_stats}
\title{Visualise Distribution of Estimated change points across Simulations}
\usage{
visualise_stats(
  detected_cp_list,
  theta,
  Time,
  p,
  num_simulations,
  method_name = ""
)
}
\arguments{
\item{detected_cp_list}{A list of numeric vectors, where each vector contains the estimated change points from a single simulation.}

\item{theta}{A numeric vector of true change point locations.}

\item{Time}{An integer specifying the time series length.}

\item{p}{An integer specifying the data dimension (not used internally but retained for consistency with other simulation functions).}

\item{num_simulations}{An integer giving the number of simulations (typically equal to \code{length(detected_cp_list)}).}

\item{method_name}{Optional character string to be included in the plot title, identifying the method used.}
}
\value{
No return value. A histogram is generated as a side effect.
}
\description{
Produces a histogram of scaled estimated change points across multiple simulations, overlaying the true change points
and tolerance bands for visual assessment of detection accuracy.
}
\details{
The function visualises the empirical distribution of estimated change points after scaling to the \eqn{[0, 1]} interval.
The histogram uses bin width \eqn{2 \log(T)/T} to reflect a theoretical detection tolerance.

True change point locations (scaled) are marked with red dotted vertical lines, and orange dashed lines denote the tolerance bands
of width \eqn{2 \log(T)/T} around each true change point.
}
\examples{
\dontrun{
# Simulated output from 100 repetitions
detected_list <- replicate(100, sort(sample(1:300, 3)), simplify = FALSE)
true_cp <- c(75, 150, 225)
visualise_stats(detected_list, theta = true_cp, Time = 300, p = 50,
                num_simulations = 100, method_name = "Example Method")
}

}
